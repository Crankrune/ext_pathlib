from setuptools import setup

setup(
    name="ext_pathlib",
    description="Extended Pathlib module.",
    version="2.0",
    py_modules=["ext_pathlib"],
    install_requires=[],
    include_package_data=True,
    zip_safe=False,
)
